// Custom components
import { ProductItem } from '../index';

//
// Component start
//
const ProductItemContainer = ({ item }) => {

    return (
        <ProductItem
            item={item}
        />
    );
}

export default ProductItemContainer;